// Archivo para exportar las URLs de las APIs

export const API_BASE_URL = "https://api.ejemplo.com";
export const CONTACT_API_URL = `${API_BASE_URL}/contact`;
export const SERVICES_API_URL = `${API_BASE_URL}/services`;

// Agrega aquí más URLs según lo necesites
